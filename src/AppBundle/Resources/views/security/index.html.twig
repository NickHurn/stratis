{% set QQnodevphone = 1 %}
{% set nousermenu = 1 %}
{% extends 'base2.html.twig' %}

{% block body %}


    <div class="container ml-auto mr-auto mt-4 mb-4 ">
        <div class="mt-5 col-lg-8 offset-lg-2 col-md-8 offset-md-2">
            <div class="row ml-auto mr-auto">
                <div class="jd col-sm-12">
                    <div class='col-12 title checkabl'>
                        <h2>Set Password for user - {{ name }}</h2>
                    </div>

                    <div class="col-12" style='clear:both; margin:30px 0 30px; color:#888'>
                        Please enter and confirm a new password below:
                    </div>

                    <div class="col-12 ">
                        {{ form_start(form) }}
                            {{ form_widget(form) }}
                        {{ form_end(form) }}

                        <!--
                            <div class="row margin-bottom" id="pwd-container">
                                <div class="col-md-4 divlabel">Password:</div>
                                <div class="col-md-6">{{ form_widget(form.password.first) }}</div>

                                <div class="col-md-4 divlabel">&nbsp;</div>
                                <div class="col-md-6" style="padding-top:10px;">
                                    <div class="pwstrength_viewport_progress"></div>
                                </div>
                            </div>
                            {% if form_errors(form.password.first) %}
                                <div class="row justify-content-end">
                                    <div class="col-8 custom-error" role="alert">
                                        {{ form_errors(form.password.first) }}
                                    </div>
                                </div>
                            {% endif %}

                            <div class="row justify-content-end passwordMessage">
                            </div>

                            <div class="row">
                                <div class="col-md-4 divlabel">Confirm Password:</div>
                                <div class="col-md-6">{{ form_widget(form.password.second) }}</div>
                            </div>
                            {% if form_errors(form.password.second) %}
                                <div class="row justify-content-end">
                                    <div class="col-8 custom-error" role="alert">
                                        {{ form_errors(form.password.second) }}
                                    </div>
                                </div>
                            {% endif %}

                            <div class="row">
                                <div class="col-md-4 divlabel">
                                    {{ form_widget(form._token) }}
                                    {{ form_widget(form.save) }}
                                </div>
                            </div>
                        -->
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block customJavascripts %}
    <script src="/js/zxcvbn.js"></script>
    <script>
        var scores = ["very poor", "poor", "average", "good", "very good"];
        var colours = ["#f00", "#f00", "#ff0", "#8f0", "#0f0"]
        $(document).ready(function () {
            $("#new_password_password_first").after("<div id='pwdmeter' style='margin-top:5px;background-color:red; color:#000; width:100%;font-weight:bold;text-align:center;border-radius:5px'>(no password)</div>");

            $('#new_password_password_first').keyup(function () {
                var val = $('#new_password_password_first').val();
                var res = zxcvbn(val);
                console.log(scores[res.score]);
                $('#pwdmeter').html(scores[res.score]);
                $('#pwdmeter').css('backgroundColor', colours[res.score]);
            });
        });
    </script>

{% endblock %}

