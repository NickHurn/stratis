
        <fieldset>
            <legend>Your new form</legend>
            {{ form_start(customForm) }}
            {{ form_widget(customForm) }}
            {{ form_end(customForm) }}
        </fieldset>
